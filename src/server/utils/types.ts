export interface UserDetails {
  username: string;
  id: string;
}

export interface LoginCredentials {
  username: string;
}

export interface LoginDetails {
  username: string;
  token: string;
  id: number;
}
